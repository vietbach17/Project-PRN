<Window x:Class="ManagementHotel.CustomerEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer" Height="320" Width="420" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Full name" VerticalAlignment="Center"/>
        <TextBox x:Name="txtFullName" Grid.Column="1" Margin="8,0,0,6"/>

        <TextBlock Grid.Row="1" Text="Phone" VerticalAlignment="Center"/>
        <TextBox x:Name="txtPhone" Grid.Row="1" Grid.Column="1" Margin="8,0,0,6"/>

        <TextBlock Grid.Row="2" Text="Email" VerticalAlignment="Center"/>
        <TextBox x:Name="txtEmail" Grid.Row="2" Grid.Column="1" Margin="8,0,0,6"/>

        <TextBlock Grid.Row="3" Text="ID number" VerticalAlignment="Center"/>
        <TextBox x:Name="txtIDNumber" Grid.Row="3" Grid.Column="1" Margin="8,0,0,6"/>

        <TextBlock Grid.Row="4" Text="Address" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAddress" Grid.Row="4" Grid.Column="1" Margin="8,0,0,6"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="90" Margin="0,0,8,0" Click="Save_Click">Save</Button>
            <Button Width="90" Click="Cancel_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
