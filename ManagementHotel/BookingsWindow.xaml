<Window x:Class="ManagementHotel.BookingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bookings" Height="520" Width="960" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.Resources>
            <Style x:Key="CenterCell" TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="TextAlignment" Value="Center"/>
            </Style>
            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>
            <Style x:Key="ToolbarButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="MinWidth" Value="90"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>
            <Style x:Key="TagStyle" TargetType="Border">
                <Setter Property="Background" Value="#FFEFF4FF"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="6,2"/>
            </Style>
            <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                <Setter Property="RowBackground" Value="White"/>
                <Setter Property="AlternatingRowBackground" Value="#FFF4F6FC"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="RowHeight" Value="30"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="16" CornerRadius="12" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Bookings" FontSize="20" FontWeight="SemiBold"/>
                    <Border Style="{StaticResource TagStyle}" Margin="12,0,0,0">
                        <TextBlock Text="Manage reservations" Foreground="{StaticResource ForegroundDimBrush}"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Signed in as" Foreground="{StaticResource ForegroundDimBrush}"/>
                    <TextBlock x:Name="txtUserBanner" FontWeight="SemiBold" Margin="6,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Padding="12" Margin="0,16,0,12" CornerRadius="12" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button x:Name="btnAdd" Click="Add_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0">Add</Button>
                    <Button x:Name="btnEdit" Click="Edit_Click" Style="{StaticResource ToolbarButton}" Margin="0,0,8,0">Edit</Button>
                    <Button x:Name="btnDelete" Click="Delete_Click" Style="{StaticResource ToolbarButton}" Margin="0,0,8,0">Delete</Button>
                    <Button x:Name="btnRefresh" Click="Refresh_Click" Style="{StaticResource ToolbarButton}">Refresh</Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <TextBlock Text="Status" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="cbStatusFilter" Width="180" SelectionChanged="StatusFilter_Changed">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="Pending"/>
                        <ComboBoxItem Content="Confirmed"/>
                        <ComboBoxItem Content="Cancelled"/>
                    </ComboBox>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12">
            <DataGrid x:Name="dg" AutoGenerateColumns="False" IsReadOnly="True" Margin="8" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Booking #" Binding="{Binding BookingId}" Width="*" ElementStyle="{StaticResource CenterCell}"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="2*"/>
                    <DataGridTextColumn Header="Room" Binding="{Binding RoomNumber}" Width="*" ElementStyle="{StaticResource CenterCell}"/>
                    <DataGridTextColumn Header="Check In" Binding="{Binding CheckInDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*" ElementStyle="{StaticResource CenterCell}"/>
                    <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOutDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*" ElementStyle="{StaticResource CenterCell}"/>
                    <DataGridTemplateColumn Header="Status" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource TagStyle}" Background="#FFE6F0FF" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" Foreground="#FF2D7FF9" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
